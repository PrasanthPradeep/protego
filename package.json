{
  "name": "protego",
  "version": "1.0.0",
  "description": "Protego - I Protect. The Shield for Workers.",
  "scripts": {
    "start": "cd backend && ../myvenv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8080",
    "dev": "cd backend && ../myvenv/bin/python -m uvicorn main:app --host 127.0.0.1 --port 8080 --reload"
  },
  "author": "PrasanthPradeep",
  "license": "MIT"
}
