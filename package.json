{
  "name": "protego",
  "version": "1.0.0",
  "description": "Protego - I Protect. The Shield for Workers.",
  "scripts": {
    "start": "cd backend && ../myvenv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8080",
    "dev": "cd backend && ../myvenv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8080 --reload"
  },
  "author": "PrasanthPradeep",
  "license": "MIT"
}
